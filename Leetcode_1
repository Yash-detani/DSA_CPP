1. Two Sum 

//first Approach

//int n = nums.size()
        // int i,j;
        // for(i=0;i<n-1;i++)
        // {
        //     for(j=i+1;j<n;j++)
        //     {
        //         if(nums[i] + nums[j] == target)
        //         return {i,j};
        //     }
        // }
        // return {-1,-1};

//second Approach

map<int,int>mpp;
        for(int i=0;i<n;i++)
        {
            int a = nums[i];
            int more = target - a;

            if(mpp.find(more) != mpp.end())
            {
                return {mpp[more],i};
            }
            mpp[a] = i;
        }

        return {-1,-1}

//third Approach
// sort(nums.begin(),nums.end());
        // int i = 0;
        // int j = nums.size()-1;

        // while(i<j)
        // {

        //     if(nums[i] + nums[j] == target)
        //     return {i,j};

        //     else if(nums[i] + nums[j] < target)
        //     i++;

        //     else
        //     j--;
        // }
        // if(i>=j)
        // return {-1,-1};
